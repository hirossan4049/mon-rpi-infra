all:
  children:
    signage:
      hosts:
        rpi-signage-01:
          ansible_host: rpi-signage-01.local
          display_rotation: 0
          bt_device_name: rpi-signage-01
  vars:
    ansible_user: signage
    ansible_become: true
    ansible_python_interpreter: /usr/bin/python3
    ansible_ssh_private_key_file: keys/signage
