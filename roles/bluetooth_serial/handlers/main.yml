---
- name: restart bluetooth
  ansible.builtin.systemd:
    name: bluetooth
    state: restarted

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart bt-spp
  ansible.builtin.systemd:
    name: bt-spp
    state: restarted
